<UserControl x:Class="UbsSampleFundApp.View.Screens.MenuScreen.MenuScreenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UbsSampleFundApp.View.Screens.MenuScreen"
             mc:Ignorable="d" d:DesignWidth="300" Height="53.069">
    <Grid>
        <StackPanel Orientation="Horizontal">
            <Button x:Name="newBond" Content="Add Bond" Margin="0,0,5,0" Command="{Binding AddNewBondStock}" Width="73" />
            <Button x:Name="newEquity" Content="Add Equity" Margin="0,0,5,0" Command="{Binding AddNewEquityStock}" Width="75" />
        </StackPanel>

    </Grid>
</UserControl>
